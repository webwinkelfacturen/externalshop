Taxes:
    - Initially fill both the creationdate and the changedate to the creationdate
    - The field percentage van be filled, both fields percentage_1 and percentage_100 are returned
    - Errormessage when adding taxes:
         - if not all fields are added return TAX100_MISSING_FIELDS
         - if a taxid is already present return TAX105_NOT_UNIQUE
         - if a taxpercentage / country combination is already present return TAX110_DUPLICATE_PERC_CTRY
